meta {
  name: ReserveSpots
  type: http
  seq: 3
}

post {
  url: {{base_url}}/sessions/{{session_id}}/bookings
  body: json
  auth: none
}

body:json {
  {
    "userId": "{{user_id}}",
    "spots": 2
  }
}

assert {
  res.status: eq 201
}

script:post-response {
  if (res.status === 201) {
    bru.setVar("booking_id", res.body.data.id);
  }
}
